# GAZEBO with gps 3dlidar imu

```bash
sudo apt-get update
sudo apt-get install libgeographic-dev
```
```bash
sudo apt-get update
sudo apt-get install ros-humble-pcl-conversions
```
```
sudo apt-get install ros-humble-gazebo-*
```
